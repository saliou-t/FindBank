<form method="post" action="{{ path('banques_controller2_delete', {'id': banque.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ banque.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
