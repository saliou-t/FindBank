{% extends "base.html.twig" %}
    {% form_theme searchForm 'bootstrap_4_layout.html.twig' %}
    
    {% block body %}
        <article class="container form-group" style="width: 40%;  padding-top: 20px;  padding-bottom: 20px">
        <h2>Formulaire de recherche</h2>
        {{ form_start(searchForm) }}
            <div class="my-3">
                {{ form_row(searchForm.nom, {'attr':{'placeholder':'Nom de la banque'}}) }}
            </div>
            <div class="my-3">
                {{ form_row(searchForm.localite, {'attr': {'placeholder' :"Nom de la localité"}}) }}
            </div>
            <div class="my-3">
                {# {{ form_widget(searchForm.submit, {'attr': {'placeholder' :"Titre de l'article"}}) }} #}
            </div>
            <button class="btn btn-success" type="submit" style=" margin-top: 20px">
                    <a>Rechercher</a>
            </button>
        {{ form_end(searchForm) }}
        </article>    
    {% endblock %}